# AI SOC CoPilot runtime dependencies.
# (Standard-library modules: csv, re, json, html, argparse, dataclasses, etc.)

requests>=2.25.0      # local Ollama HTTP client
pydantic>=2.0         # structured / validated evidence-grounded findings
pyyaml>=6.0           # config.yaml loading
